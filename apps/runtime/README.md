# FlowOS Runtime

FastAPI + LangGraph execution engine. Deploy to Railway.

## Setup
poetry install
cp .env.example .env
poetry run uvicorn main:app --reload

## Environment variables (see .env.example)
